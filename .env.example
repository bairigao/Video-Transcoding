# Application Configuration
PORT=3000
NODE_ENV=production
JWT_SECRET=your-secure-jwt-secret-change-this-in-production

# YouTube API Configuration
YOUTUBE_API_KEY=your-youtube-api-key-here

# Database Configuration (MariaDB)
DB_HOST=mariadb
DB_PORT=3306
DB_NAME=video_transcoding
DB_USER=video_user
DB_PASSWORD=secure_video_password_change_this
DB_ROOT_PASSWORD=secure_root_password_change_this

# File Storage Configuration (Docker paths)
UPLOADS_DIR=/app/uploads
TRANSCODED_DIR=/app/transcoded